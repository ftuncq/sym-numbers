<div class="htfd6z d-flex mb-3">
	{{ form_start(form, { 'action': path('search', { 'program_slug': app.request.attributes.get('program_slug') }) }) }}
	<div class="inner-form">
		<div class="input-field first-wrap">
			{{ form_widget(form, {'attr': {'id': 'course-autocomplete'}}) }}
		</div>
		<div class="input-field second-wrap">
			<button type="submit" class="fnkrdt"><i class="fa-solid fa-magnifying-glass"></i></button>
		</div>
	</div>
	{{ form_end(form) }}
</div>